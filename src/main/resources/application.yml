spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mini-shop?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    baseline-on-migrate: true

mybatis:
  config-location: classpath:db/mybatis/config.xml
